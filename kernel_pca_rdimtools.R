# Kernel PCA with Rdimtools

library(Rdimtools)

# kp1.1 = kpca(as.matrix(t1.1), kernel = "polydot", kpar = list(degree = 3, offset = 1))

## COKUYOR
# fit.dokpca.11 = do.kpca(
#                    X = as.matrix(t1.1),
#                    ndim = 9,
#                    preprocess = "cscale",
#                    kernel = c("polynomial", 1, 3))



